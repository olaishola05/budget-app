<div class='login'>
    <%= form_with model: @transacts, url: category_transacts_path do |form| %>
  <% if transact.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(transact.errors.count, "error") %> prohibited this transact from being saved:</h2>

      <ul>
        <% transact.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, placeholder: 'E.g Snacks' %>
  </div>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.number_field :amount, placeholder: '$' %>
  </div>

  <div>
    <%= form.label :categories, style: "display: block" %>
    <%= form.select :category.name, @categories.map{ |category| [category.name, category.id] } %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

</div>