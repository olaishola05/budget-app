<h1>Transacts</h1> <%= current_user.name%>

<h2>Total Transaction: <%= @transacts.map {|t| t.amount}.sum %></h2>
<div id="transacts">
  <% @transacts.each do |transact| %>
    <%= render transact %>
  <% end %>
</div>
<% if can? :create, Transact%>
    <%= link_to "New transact", new_category_transact_path %>
<%end%>

<%= link_to 'Categories', categories_path%>
<%console%>